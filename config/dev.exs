import Config

# Dev: use manual node connection or local clustering
# Connect nodes manually: Node.connect(:"other@127.0.0.1")

# Optional: enable local clustering via Gossip for dev
# config :libcluster,
#   topologies: [
#     local: [
#       strategy: Cluster.Strategy.Gossip
#     ]
#   ]
